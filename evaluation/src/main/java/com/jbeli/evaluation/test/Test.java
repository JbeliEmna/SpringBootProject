package com.jbeli.evaluation.test;
import com.jbeli.evaluation.interview.Interview;
import com.jbeli.evaluation.pieceJointe.PieceJointe;
import io.swagger.annotations.ApiModel;
import io.swagger.annotations.ApiModelProperty;
import jakarta.persistence.*;
import lombok.*;
import org.hibernate.validator.constraints.Range;
import org.springframework.data.annotation.CreatedDate;

import java.util.Date;
import java.util.HashSet;
import java.util.Set;

@Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
@Builder
@Entity
@ApiModel("Contains all the attributes required under Test entity")
public class Test {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    @ApiModelProperty("This the unique ID of the Test , will be generated by the system")
    private Integer id ;

    @ApiModelProperty("This the unique note of the Test ")
    private int note;

    @ApiModelProperty("This the date of the Test ")
    private Date date;

    @ManyToOne
    @JoinColumn(name = "interview_id")
    private Interview interview;

    @OneToOne(mappedBy = "test")
    private PieceJointe pieceJointe ;

}
