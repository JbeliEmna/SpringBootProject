eureka :
  instance :
    hostname : localhost
  client :
    service-url :
      defaultZone : http://localhost:8761/eureka

server:
  port : 8090

spring :
  application:
    name : gestionUtilisateurs
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/gestion-utilisateur-db?createDatabaseIfNotExist=true
    username: postgres
    password: Emna@2024
  jpa :
    hibernate:
      ddlAuto: update
    show-sql: true
    database: postgresql
    databasePlatform: org.hibernate.dialect.PostgreSQLDialect


main:
  allow-circular-references: true